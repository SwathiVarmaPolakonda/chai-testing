var chai = require("chai");
var expect = chai.expect;

var { sort } = require("../sort");
var assert = require("assert");
describe("sorting", function() {
  it("should sort the array elements", function() {
    //var arr = [];
    //expect.(arr).to.have.members(sort([1, 2, 3]), [3, 2, 1]);
    // expect.deepEqual(sort([1, 2, 3]), [3, 2, 1]);
    // expect([1, 2, 3]).to.include.members([3, 2, 1]);
    expect(sort[1, 2, 3])).to.equal([3, 2, 1]);
    //assert.equal(sort([1, 2, 3]), [3, 2, 1]);
  });
});
